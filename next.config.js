/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['bfiiwboaezyxtvnxxhes.supabase.co', 'images.unsplash.com'],
  },
}

module.exports = nextConfig 